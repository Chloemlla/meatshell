id: io.github.chloemlla.meatshell
runtime: org.freedesktop.Platform
runtime-version: "25.08"
sdk: org.freedesktop.Sdk
command: meatshell
default-branch: stable

finish-args:
  - --share=ipc
  - --share=network
  - --socket=wayland
  - --socket=fallback-x11
  - --device=dri
  # MeatShell uploads/downloads arbitrary user-selected files and can open
  # local terminal sessions. Keep host-home access until those paths use
  # portals throughout the application.
  - --filesystem=home
  # Required for serial sessions (/dev/ttyUSB*, /dev/ttyACM*, and similar).
  - --device=all

modules:
  - name: meatshell
    buildsystem: simple
    build-commands:
      - install -Dm755 meatshell /app/bin/meatshell
      - install -Dm644 io.github.chloemlla.meatshell.desktop /app/share/applications/io.github.chloemlla.meatshell.desktop
      - install -Dm644 io.github.chloemlla.meatshell.metainfo.xml /app/share/metainfo/io.github.chloemlla.meatshell.metainfo.xml
      - install -Dm644 icon.png /app/share/icons/hicolor/512x512/apps/io.github.chloemlla.meatshell.png
      - install -Dm644 THIRD_PARTY_NOTICES.md /app/share/doc/meatshell/THIRD_PARTY_NOTICES.md
    sources:
      - type: file
        path: ../../target/x86_64-unknown-linux-gnu/release/meatshell
        dest-filename: meatshell
      - type: file
        path: io.github.chloemlla.meatshell.desktop
      - type: file
        path: io.github.chloemlla.meatshell.metainfo.xml
      - type: file
        path: ../../assets/icon@512.png
        dest-filename: icon.png
      - type: file
        path: ../../THIRD_PARTY_NOTICES.md
